<% #@file Takes a tag and solr_document field and renders multiple fields. %>

<% sd = @presenter.solr_document %>
<% sd.public_send(field_name).each do |value| %>
  <%= content_tag(tag_name, value) %>
<% end unless(sd.public_send(field_name).nil?) %>
