      <div class="pull-left tdl-search-widgets">
        <span class="glyphicon glyphicon-arrow-left glyph-left" aria-hidden="true"></span>
        <% if controller.action_name == 'fa_overview'%>
          <a href="<%= show_collection_link(params[:id]) %>">Collection Home</a>
        <% elsif controller.action_name == 'fa_series' %>
          <a data-turbolinks='false' href="<%= show_finding_aid_link(params[:id]) %>"><%= @collection_title %></a> > <%= @series_full_title %>
        <% end %>
      </div>

      <% if controller.action_name == 'fa_overview'%>
        <div class="pull-right tdl-search-widgets" style="display: none;">    <!-- display: none; for now until pdfs become available -->
          <span class="glyphicon glyphicon-file glyph-left" aria-hidden="true"></span>
          <a href="/file_assets/pdf/<%= params[:id] %>"></i>Download Collection Guide (PDF)</a>    <!-- this route is probably wrong -->
        </div>
      <% end %>
